# サンプルシステム6
# 関数とかも使うテスト 始値が素数だったら買う

def Is_primal(n)
    i=2
    while (i<(n/2))
        if (n%i == 0)
            # 割り切れたので素数ではない
            return 0
        end
        i = i + 1
    end
    return 1
end

if Is_primal(Open)
    Buy(Open, 100)
end

# 実行結果
# break文が書ける。 continue も書けるらしい。
# 関数名の先頭は大文字でないといけない
# 関数型は明示しなくてもよく、戻り値を返さなかったら自動的にnullを返す
# インクリメント、デクリメント、ビット演算の演算子は存在しない（不便！）